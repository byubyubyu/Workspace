public enum Team
{
    None,
    Blue,
    Red,
    Green
}

public enum StatType
{
    HP,
    Speed,
    AttackDamage,
    AttackRange,
    DetectionRange,
    AttackInterval,
    RepairAmount,
    BuildPower
}

public enum BuildingType
{
    CityHall,
    Barracks,
    Turret
}

public enum ResourceType
{
    Gold,
    Mana
}

public enum TargetTag
{
    Minion,
    Player,
    Building
}